//////////////////////蛇形填数////////////////////////
#include<stdio.h>
#include<string.h>
#define maxn 20
int a[maxn][maxn];
int main(){
	int n,x,y,tot=0;
	scanf("%d", &n);
	memset(a,0,sizeof(a));
	tot=a[x=0][y=n-1]=1;//简便
}
